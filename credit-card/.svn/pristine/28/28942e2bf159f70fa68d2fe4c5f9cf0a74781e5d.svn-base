package com.pay.aile.bill.utils;

import javax.annotation.Resource;

import org.springframework.stereotype.Component;

import com.pay.aile.bill.service.CreditErrorLogService;

@Component
public class ErrorLogUtil {

    @Resource
    private CreditErrorLogService creditErrorLogService;

    public void analyzeErrorLog(Exception e) {
        try {
            // creditErrorLogService.saveAnalyzeErrorLog(creditEmail.getEmail(),
            // Long.valueOf(creditEmail.getUserId()),
            // e.getMessage());
        } catch (Exception ee) {

        }
    }

}
